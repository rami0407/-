# 🔐 نظام الحماية - لوحة التحكم

## كلمة المرور الحالية

```
musherfe318212
```

**⚠️ مهم جداً**: احتفظ بهذه الكلمة في مكان آمن ولا تشاركها مع الطلاب!

---

## 🔒 كيف يعمل النظام؟

### عند محاولة فتح لوحة التحكم:

1. **يظهر مربع حوار** يطلب إدخال كلمة المرور
2. **إذا أدخل المستخدم الكلمة الصحيحة**:
   - ✅ يتم السماح بالدخول
   - يتم حفظ حالة تسجيل الدخول في الجلسة الحالية
   - يظهر زر "تسجيل الخروج" في الأعلى

3. **إذا أدخل المستخدم كلمة خاطئة**:
   - ❌ يتم رفض الدخول
   - يتم إعادة التوجيه تلقائياً إلى صفحة النموذج

4. **إذا ضغط "إلغاء"**:
   - يتم إعادة التوجيه إلى صفحة النموذج

---

## 🔓 تسجيل الخروج

عند الانتهاء من العمل:
1. اضغط على زر **"🔓 تسجيل الخروج"** في أعلى الصفحة
2. أكّد عملية تسجيل الخروج
3. سيتم إعادة توجيهك إلى الصفحة الرئيسية

---

## 🔄 مدة الجلسة

- **الجلسة تستمر**: طوال فترة فتح المتصفح
- **تنتهي الجلسة**: عند إغلاق المتصفح أو النافذة
- **بعد انتهاء الجلسة**: سيُطلب إدخال كلمة المرور مرة أخرى

---

## 🔧 تغيير كلمة المرور

إذا أردت تغيير كلمة المرور:

### الطريقة:

1. افتح ملف `dashboard.html` في محرر النصوص
2. ابحث عن السطر:
   ```javascript
   const CORRECT_PASSWORD = "musherfe318212";
   ```
3. غيّر الكلمة إلى ما تريد:
   ```javascript
   const CORRECT_PASSWORD = "كلمة_المرور_الجديدة";
   ```
4. احفظ الملف
5. ارفعه على GitHub مرة أخرى

---

## 🛡️ مستوى الأمان

### ✅ مناسب لـ:
- الاستخدام المدرسي الداخلي
- حماية من الوصول العرضي
- منع الطلاب من الوصول للبيانات

### ⚠️ ليس مناسباً لـ:
- البيانات الحساسة جداً
- الاستخدام المالي أو الطبي
- البيانات التي تتطلب تشفيراً عالي المستوى

---

## 🔐 خيارات أمان إضافية (اختياري)

### 1. استخدام Firebase Authentication

للحصول على أمان احترافي، استخدم Firebase Auth:

```javascript
const auth = firebase.auth();

auth.onAuthStateChanged((user) => {
    if (!user) {
        window.location.href = "login.html";
    }
});
```

### 2. جعل المستودع خاصاً

في GitHub:
- Settings → Danger Zone → Change visibility
- اختر "Private"
- شارك الرابط فقط مع المعلمين

### 3. استخدام .htaccess (للخوادم الخاصة)

إذا كنت تستضيف على خادم خاص بدلاً من GitHub Pages:

```apache
AuthType Basic
AuthName "Dashboard Access"
AuthUserFile /path/to/.htpasswd
Require valid-user
```

---

## 📝 سجل تغيير كلمات المرور

استخدم هذا الجدول لتوثيق التغييرات:

| التاريخ | الكلمة القديمة | الكلمة الجديدة | المسؤول |
|---------|----------------|----------------|----------|
| ___/___/___ | ____________ | musherfe318212 | رامي |
| | | | |
| | | | |

---

## ❓ أسئلة شائعة

### س: هل يمكن للطلاب معرفة كلمة المرور من الكود؟
ج: نعم، إذا فتحوا "View Source" في المتصفح. لذلك:
- غيّر الكلمة بشكل دوري
- لا تجعلها سهلة التخمين
- للأمان الكامل، استخدم Firebase Auth

### س: كم مرة يجب تغيير كلمة المرور؟
ج: 
- **للاستخدام العادي**: كل 3-6 أشهر
- **إذا تسربت**: فوراً
- **للاستخدام المكثف**: شهرياً

### س: ماذا لو نسيت كلمة المرور؟
ج: افتح ملف `dashboard.html` وابحث عن:
```javascript
const CORRECT_PASSWORD = "...";
```

### س: هل يمكن إضافة عدة مستخدمين بكلمات مرور مختلفة؟
ج: نعم، لكن يتطلب كود أكثر تعقيداً. مثال:

```javascript
const USERS = {
    "المدير": "musherfe318212",
    "المعلم1": "password123",
    "المعلم2": "pass456"
};

const username = prompt("اسم المستخدم:");
const password = prompt("كلمة المرور:");

if (USERS[username] === password) {
    // السماح بالدخول
}
```

---

## 🚨 تحذيرات أمنية

### ⚠️ لا تفعل:
- ❌ لا تشارك كلمة المرور في مجموعات عامة
- ❌ لا تكتبها على الورق وتتركها مرئية
- ❌ لا تستخدم كلمات مرور سهلة التخمين (123, password, etc.)
- ❌ لا تستخدم نفس كلمة المرور لحسابات أخرى

### ✅ افعل:
- ✅ غيّر الكلمة دورياً
- ✅ احفظها في مدير كلمات المرور
- ✅ شاركها فقط مع المعلمين الموثوقين
- ✅ سجّل الخروج عند الانتهاء

---

## 📞 الدعم

إذا واجهت مشاكل في نظام الحماية:
1. راجع ملف `FAQ.md`
2. تأكد من إدخال الكلمة بشكل صحيح (حساسة لحالة الأحرف)
3. جرّب متصفحاً آخر
4. امسح الـ Cache والـ Cookies

---

**💙 نظام محمي، طلاب آمنون، بيانات محفوظة!**

آخر تحديث: ديسمبر 2024
